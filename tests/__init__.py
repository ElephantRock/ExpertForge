"""ExpertForge test suite.

Established by Milestone 0 (Issue #13 owns the test harness; Issue #4 seeds a
minimal test so `pytest` runs from a clean checkout). Component tests for later
Milestone 0 subsystems (config, run identity, provenance, seeds, logging,
artifacts, checkpoints, manifests) are added by their respective issues.
"""
