"""D0.1b deterministic training-data pipeline: identity functions.

Pure, independently testable functions implementing the frozen D0.0 contract's
domain-separated hash formulas for split, training-order, and validation-order
derivation. These are the first implementation deliverable — golden-vector
tests prove exact byte-level semantics before any materialization code runs.
"""
