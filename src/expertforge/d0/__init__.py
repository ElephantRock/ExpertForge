"""Controlled D0 implementation packages.

The package intentionally performs no eager imports. Data and model dependencies
are optional surfaces and must not become requirements of the Milestone 0 base
installation merely because :mod:`expertforge.d0` is imported.
"""

from __future__ import annotations
